[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.build]
cmds = ["npm install", "npm run build"]

[phases.start]
cmds = ["npm run serve"]

[static]
  dir = "build"
  cmd = "npx serve -s build -p 3000"